# MEMORY.md — 학습 메모리

> 에이전트가 자율적으로 편집한다. 작업하며 얻은 교훈·패턴·도구 특성을 누적한다.
> 형식: 한 항목 = 한 줄(또는 짧은 단락). 날짜는 절대표기(YYYY-MM-DD).
> 매 작업 종료 시 새 항목이 있는지 점검한다 (LOOP.md 참조).

## 교훈 (Lessons)

- 2026-06-09 — 에이전트 홈은 `/Users/karla/cowork` 하나로 통일. 과거 `~/Documents/cowork`에 만든 버전은 통합 후 폐기(리다이렉트만 남김). 앞으로 모든 메모리/스킬은 이 폴더에만 쓴다.
- 2026-06-09 — "X를 Y처럼 만들어줘" 작업의 교훈: Y의 **요약 자료(README·랜딩)로 끝내지 말고 1차 운영 자료**(실제 구조·문서·소스)까지 파야 한다. 처음에 헤르메스 GitHub README만 보고 원칙에서 구조를 *재구성*하다가 실제 산출물(CLAUDE.md·LOOP.md·5단계·스킬 카테고리)을 놓쳤다. 충실성(fidelity)이 목표면 내 설계보다 원본 대조가 먼저다. 검증 단계에 "원본과 1:1 비교"를 반드시 넣는다.
- 2026-06-15 — [디버깅 규칙] 헛도는 반복 판별(멈춤 신호) = ①새 정보 없이 동일 형태 수정 반복 ②관련 코드 단위 미독 ③비용 큰 행동을 '수정 반영' 검증 전 실행. 셋 중 하나면 멈추고 물러나 통독·진단. **새 증거를 만드는 검증·재시도는 루프 아님(권장)** — 회피 대상은 '검증'이 아니라 '근거 없는 동일 시도'. 보조: 최소 diff보다 확실한 해법 우선(BSD sort `-V` 불확실 → 처음부터 `-n`); 에러가 *매번 정확히* 0바이트면 '순서손상' 아닌 '입력 0건' 의심. 사례: aihubshell 0바이트를 함수 통독 없이 정렬만 추측 패치하며 ~8분 다운로드 반복 소모(사용자 '무한루프' 지적), 진짜 원인은 `printf %q` 한글 이스케이프(도구특성 참조).
- 2026-06-15 — [리허설 디버깅] AI Hub 71388 brailleBlock 객체탐지 파이프라인 3단계(DataLoader/bbox시각화/FasterRCNN 1epoch) 전부 통과. 핵심 교훈: ①라벨이 bbox 아닌 **polygon** → min/max로 xyxy 유도(+경계클리핑+degenerate 가드). ②EXIF orient=6 이미지 있으나 라벨이 stored 공간 → **`exif_transpose` 부르면 좌표 깨짐**(부르지 말 것). bbox 시각화로 점자블록에 정확히 정합 육안 확인. 스키마노트 v1 승격. (constgx/vision/)
- 2026-06-16 — [교훈, '산출물로 판정' 원칙 보강] "모니터 스트림/세션 종료 ≠ 작업 완료". tmux 모니터 스트림이 닫혀도 컴퓨터·세션은 살아있을 수 있음(전경 tail/attach가 종료됐거나, 루프가 tmux 세션의 메인 명령이라 끝나며 세션 자동파기). 완료는 그 신호가 아니라 **산출물로 판정**: 상태파일 DONE 수, 버킷 객체 수·용량, 잔존 큐 0, 로컬 정리 0. 사례: 건설GX 전량 적재 종료를 스트림 단절로 추정 → 상태표(76 DONE)·`gsutil du`(362.2GiB/80객체)로 *검증*해 확정.
- 2026-06-17 — [교훈] 문서가 참조하는 코드 경로는 작업 전 실재(ls)로 검증하라. GAP-ANALYSIS.md·오케스트레이터 체크포인트가 `constgx/agents/`(orchestrator.py·verifier.py·tools.py)를 "기존 골격"으로 *기술*했으나 실제 디스크엔 없었음(설계 문서의 "내 로직 = …" 서술을 코드 실재로 오인하면 잘못된 전제로 작업 시작 → 헛도는 '슬롯 매핑' 시도). 부팅/점검 단계에서 참조 경로를 `find`/`ls`로 먼저 확인. 이 프로젝트는 문서↔코드 갭이 반복될 소지(설계 선행, 코드 후행)라 재사용 빈도 높음.
<!-- 예) 2026-06-09 — PDF 표 추출 시 pdfplumber가 camelot보다 한글 셀 정렬에 강함. -->

## 패턴 (Patterns)

- 2026-06-14 — 프로젝트는 SDD 초안 순서로 **지속적으로 학습하며 진행**한다: 문제·가치 가설(비전) → 경량 데이터 탐색 → 가상 이해관계자·유스케이스 정의 → 기능 요구명세(FRD) → 비기능 요구(NFR) → SPEC.md(SDD) 문서화 → 수직 슬라이스(MVP).
- 2026-06-20 — [정본분리·갱신] 3개 레포 분리 규약(2026-06-14 cowork/constgx 2분할을 대체): ①**cowork-agent**(github IKWHI-OSS/cowork-agent) = 에이전트 메모리·스킬(SOUL/LOOP/MEMORY/USER)만 = `/Users/karla/cowork`. ②**Building-Infra**(IKWHI-OSS/Building-Infra) = 범용 인프라 오케스트레이션 코드·설계지식만(`constgx/agents/orc`+build-log), 프로젝트 종료 후에도 재사용. ③**Pred-FirefromElec**(IKWHI-OSS/Pred-FirefromElec) = 기상기후×전력설비×가연환경 화재위험도 예측 **프로젝트** 산출물(데이터 맵·스키마·적재현황·RAG 매니페스트·vision). 판단기준 = "도구·작업방식 교훈이면 cowork / 범용 인프라면 Building-Infra / 이 프로젝트 산출물·상태면 Pred-FirefromElec". 라우팅 상세=`cowork/REPO-MAP.md`.

- 2026-06-20 — [정본분리·포인터] 인프라 오케스트레이션(orc 엔진) **설계지식의 정본 = `Building-Infra` 레포**(코드 `constgx/agents/orc` + 설계문서 build-log). cowork MEMORY엔 본문 안 둠. 과거 여기 있던 'LangGraph ORC 순수라우터 2단분리'·'PoC 3스레드 방법론' 2건은 build-log로 이관함(2026-06-20). 인프라 설계 회상은 Building-Infra에서.
<!-- 예) 사용자는 보고서를 항상 "결과 요약 → 상세 → 다음 단계" 순으로 원함. -->

## 도구 특성 (Tool Notes)

- 2026-06-14 — aihubshell 배치 공간 모델(중요): download.tar 받아 **압축해제하는 순간 tar+추출본이 동시 존재 → peak ≈ 배치크기×2**. "여유의 절반" 규칙은 위험. 안전식 `B_max = 0.40×free`(peak 0.80 + 버퍼 0.20). 269GB free → 권장 ~80GB/배치. 배치 수 고정 말고 파일트리 실측+실시간 df로 적응 산정(`plan_batches.py` 빈패킹). `-mode l` 출력 파싱 시 datasetkey 자신/헤더 줄을 filekey로 오인하지 않게 제외할 것.
- 2026-06-14 — aihubshell 통짜 다운로드 취약성(⚠️원인 오진 — 2026-06-15 정정 항목 ↓ 참조): 큰 파일을 1GB `.partN`(오프셋명, 예 `.zip.part0`·`.zip.part1073741824`) 조각으로 받아 `download.tar`로 묶어 보냄 → 받은 뒤 압축해제+조각 병합해야 원본 `.zip` 완성. "Download successful/HTTP 200"은 *전송*만 의미하고, 그 뒤 해제·병합이 중단(노트북 잠자기·세션종료·공간부족)되면 **0바이트 zip stub만 남고 데이터 소실**. 해법: ①filekey 1개씩 받기 ②tmux/nohup으로 감싸 무인 실행 ③받은 뒤 `merge_parts.sh`로 `.part` 재귀 병합 ④`unzip -t`로 무결성 확인해야 *진짜* 데이터 ⑤검증 통과 후에만 로컬삭제. 속도 ~12MB/s throttle(414GB≈다운로드만 ~10h). 병렬 다운로드는 계정/IP throttle·차단 위험으로 비권장. (constgx/scripts/merge_parts.sh)
- 2026-06-14 — 코워크 bash는 맥이 아니라 **격리된 리눅스 샌드박스**(별도 ~9.6GB VM). `df`·맥 로컬 디스크·맥에 받은 데이터를 직접 못 봄. 맥 상태는 사용자 보고 또는 computer-use 필요. 연결 폴더(mnt) 밖은 안 보임.
- 2026-06-14 — 멀티에이전트 설계 체크리스트(한빛 서지영 3부작 대조 결과, 내 골격이 놓친 계층): ①AgentContext(단계간 실행상태 공유 — 핵심 구조), ②운영 3축[보안: 최소권한·정책검토에이전트·룰기반 안전필터(rm -rf/DROP TABLE 사전차단)·HITL·감사로깅 / 품질: LLM-as-a-Judge·회귀평가 / 비용: 컨텍스트 최소화·캐싱·모델 계층화(SLM↔대형)], ③동적 플래너 replan·개선율 종료조건, ④유형 다양성(계층형·경쟁형·이벤트기반·병렬). 룰기반 verifier는 *사후 출력검증*일 뿐 *사전 안전검증* 아님. 파괴적 작업 프로젝트에선 HITL·안전필터 누락이 최우선 리스크. (constgx/agents/GAP-ANALYSIS.md)
- 2026-06-14 — 자기검증 루프(self-verification) 골격 패턴: `Step(action, params, acceptance)` 삼중항 → 도구호출 → `verify(result, acceptance)`가 원인코드 반환 → DIAGNOSIS 테이블로 (retriable?, corrective) 매핑 → 보정 파라미터 합쳐 bounded 재시도 → 판정/보정 전부 JSONL 로그. 실패를 1급 시민으로 다루는 게 핵심. (constgx/agents/ 참조)
- 2026-06-14 — AI 허브 데이터 API 다운로드: 공식 CLI `aihubshell`(`curl -o aihubshell https://api.aihub.or.kr/api/aihubshell.do`) 사용. `dataSetSn`=`datasetkey`. 흐름: `-mode l -datasetkey N`(파일트리·파일SN, 키 불필요) → `-mode d -datasetkey N -aihubapikey '키' -filekey all|3,5,7`. 다운로드는 download.tar 한 덩이로 받아 자동 압축해제+`.part` 병합. `curl -L -C -`라 같은 폴더 재실행 시 이어받기(resume). 끊김 잦으면 `-filekey`로 파일 단위 분할이 안전. 활용신청 승인 후에만 API키 발급/다운로드 가능. 키 특수문자는 홑따옴표로 감쌈. macOS는 curl/tar 기본 내장이라 그대로 동작.
- 2026-06-09 — 코워크 파일 삭제: `request_cowork_directory`로 추가 연결한 폴더는 `allow_cowork_file_delete` 승인 후 삭제 가능. 그러나 앱 기본 워크스페이스 마운트(예: `~/Documents/cowork`)는 mount 인식이 안 돼 삭제 불가 → 사용자가 Finder에서 수동 삭제해야 함.
- 2026-06-15 — [교훈·확정] 외부 도구 성공/실패는 **로그 텍스트가 아니라 산출물 존재로 판정**하라. aihub_ingest.sh가 `grep -qiE '...|502'`로 다운로드 로그를 스캔했는데 curl 진행률의 바이트수치(`502M`)의 "502"에 오매칭 → 정상 10GB 다운로드를 "해외IP/502"로 거짓양성 ABORT. 수정: ①성공=새 0바이트아닌 zip 존재로 먼저 판정(있으면 로그 검사 자체 스킵) ②에러 스캔은 zip 없을 때만 + HTTP 상태문맥 고정(`HTTP/[0-9.]+ 502` 등, 맨 '502' 금지). 일반화: 텍스트 매칭 게이트는 좁고 문맥고정으로. (504527 Cursor eval이 잡음 — dry-run의 가치)
- 2026-06-15 — [패턴] 회복탄력 러너에 **기존 유효 산출물 재사용 가드**: 재실행 시 그 작업의 기대 산출물이 이미 있으면(이름패턴 매칭) 비싼 재생성(다운로드) 스킵→검증부터. 단 첫 시도에만 + 이름패턴으로 타작업 잔존본 오재사용 방지(재시도엔 새로 생성). preflight는 시크릿/형식(KEY 존재, BK=gs://)도 검증해 raw bash 에러(`${X:?}`)로 무로그 종료 방지. **무한루프 방지 3원칙**=①모든 작업단위 종료(DONE/FAILED) ②유계재시도+새증거(corrective 적용 후에만 재시도) ③연속실패 차단기(K회면 halt). 미진단 에러는 재시도 말고 FAILED+halt→진단표에 추가(자기학습).
- 2026-06-15 — aihubshell 0바이트 병합 실패의 진짜 원인(확정, 소스 직접 분석): merge_parts()가 `escaped_prefix=$(printf '%q' "$prefix")` 후 `find -name "${escaped_prefix}.part*"`로 조각을 찾는데, macOS 기본 bash 3.2의 `printf %q`가 한글 prefix(예 `TS_건설자재_brailleBlock.zip`)를 8진수(`\352\261…`)로 이스케이프 → find 패턴이 실제 한글 파일명과 불일치 → 0개 반환 → 빈 cat → **항상 정확히 0바이트**(잠자기·공간·정렬 -zV/-k2V 전부 무관, 간헐 아님). 해법(패치): 병합 줄을 `ls "${target_dir}/${prefix}".part* | sed 's/.*\.part//' | sort -n | while read off; do cat "...part${off}"; done > 출력`로 교체 — 실제 prefix 글롭으로 find/%q 제거 + macOS-safe `sort -n`(BSD sort는 `-V` 불확실). 삭제는 `[ -s 출력 ] && rm`으로 가드(0바이트면 조각 보존 → 재다운 불필요). aihubshell은 `/usr/local/bin`(root소유) → `sudo python3` 인플레이스 패치, `.bak` 백업. **1회 패치로 414GB 전체 filekey에 적용**. 검증: filekey 504524 brailleBlock 15GB `unzip -t` 통과(2026-06-15). ⚠ aihubshell 재설치(`curl ... aihubshell.do`) 시 패치 소실 → 재적용 필요.
- 2026-06-15 — 코워크 리눅스 샌드박스에서 PyTorch 돌리기(확정): ①디스크 ~4.4GB free만 가용 → CUDA 풀빌드 불가. ②`download.pytorch.org/whl` 인덱스는 **프록시 403 차단** → 기본 PyPI 사용. ③최신 torch는 aarch64에도 nvidia cu13 번들(cublas 542MB 등)로 디스크 폭발 → **`torch==2.2.2 torchvision==0.17.2`로 핀**하면 CPU-only aarch64 휠(경량). ④torch 2.2.x는 numpy<2 빌드 → `pip install "numpy<2"` 안 하면 `to_tensor`에서 "Numpy is not available". ⑤사전학습 weights도 download.pytorch.org라 차단 → 리허설은 `weights=None, weights_backbone=None`(랜덤초기화로 loss유한·역전파 검증엔 충분). ⑥CPU 속도 위해 `fasterrcnn_resnet50_fpn(min_size=256,max_size=400)`로 내부 입력 축소.
- 2026-06-15 — [박제] 71388 전체 filekey 맵을 `knowledge/aihub-71388-filekey-map.md`에 확정 저장(트리 `~/aihub_dl/tree_71388.txt`). **다시 `aihubshell -mode l` 조회하지 말 것**(사용자가 반복 조회 지적). 건설자재 TS=504524~504543(메인 이미지 ~300GB), TL=504566~504585, VS=504608~504627, VL=504650~504669. 504524/504566(brailleBlock) ✅로컬검증.
- 2026-06-15 — [박제·주의] 사용자 표준 수동 다운로드 프롬프트: `cd ~/aihub_dl && rm -rf 237* ; aihubshell -mode d -datasetkey 71388 -aihubapikey "$KEY" -filekey <FK> 2>&1 | tee ~/aihub_<FK>e.log`. ⚠ **`rm -rf 237*`가 직전 다운로드본을 삭제** → 다음 filekey 전에 GCS 업로드 필수. 매 세션 이 프롬프트를 재유도하지 말고 그대로 제공(사용자 비용절감). brailleBlock(504524)은 현재 GCS 미적재 로컬 유일본 → rm 전 업로드 안 하면 16GB 검증본 소실.
- 2026-06-15 — [샌드박스 실행 제약] 코워크 bash는 매 호출이 `--die-with-parent` bwrap 세션 → **nohup/setsid 백그라운드가 호출 종료 시 같이 죽음**(호출 간 생존 불가, pip가 캐시로 빨리 끝났을 때만 우연히 됐던 것). 긴 작업은 ①한 호출 ~44초 안에 끝나게 줄이거나 ②단계 분리(env 플래그)로 각 단계를 개별 호출에 맞춰라. CPU 학습은 입력해상도·샘플수를 줄여 윈도우에 맞추는 게 정석. (헛돌이 방지: 백그라운드 생존 가정하고 sleep+polling 반복 = 멈춤 신호) [박제 이유] 이 세션에서 백그라운드 생존을 가정해 nohup+sleep+polling을 여러 번 반복하다 헛돌았음 → 직접 재발한 비용 큰 실수라, 박제 안 하면 다음 세션에서 같은 함정(긴 작업을 백그라운드로 던지고 재진입 시 결과 기대)에 또 빠짐. 코워크에서 "긴 외부작업"을 만날 때마다 적용되는 환경 불변 제약이라 재사용 빈도도 높음.
- 2026-06-16 — [현황·확정] 건설GX 71388 건설자재 **전량 적재 완료**: 4계층 풀세트(TS/TL/VS/VL 각 20 filekey) = `gs://constgx-aihub-237`(서울)에 .zip 80객체·362.2GiB. 러너 `aihub_ingest.sh` 무사고 완주(75/75 큐 DONE, 상태표 76 DONE=504527 평가본 포함, FAILED/RETRY/REDOWNLOAD/ABORT 0). 전 건 2게이트(unzip -t → upload → size-match) 후 삭제, 로컬 237* 0, 디스크 293GB 여유. 총 7h37m. 504527 eval 수정(산출물기반 성공판정·HTTP문맥 502·재사용가드·preflight KEY/BK)이 전량 내내 거짓양성 0. → **적재현황 재조회 말 것**(`aihubshell -mode l` 금지, 상태표·`gsutil ls`로 확인).
- 2026-06-16 — aihubshell 단일전송은 **AI Hub 피크시간 throttle로 동급 크기가 3~7배 변동**(예 TS 504533/541/542/543 각 28/38/54/37min, RETRY 0이라 러너 재시도 아님). 러너 `est_min`은 다운로드만 추정 → 업로드/검증 미포함이라 대용량서 실제와 큰 괴리(504542: 11min 추정 vs 54min 실측). 무인 모니터링 기대치 관리하려면 **업로드 추정 한 줄 추가 권장**(패치 후보). 무손상·전건 성공이면 성능 변동 자체는 무조치.
- 2026-06-18 — [도구특성] 코워크 샌드박스에서 LangGraph PoC 검증 가능: `pip install --break-system-packages langgraph langgraph-checkpoint-sqlite`로 langgraph 1.2.5 설치됨(StateGraph/START/END/SqliteSaver/InMemoryStore 임포트 OK). 노트북 e2e 검증은 `jupyter` CLI가 PATH에 없으니 **`python3 -m nbconvert --to notebook --execute`**로(출력은 변환된 ipynb의 cells[].outputs에서 추출). SqliteSaver는 `sqlite3.connect(":memory:", check_same_thread=False)` 넘겨 생성. ⚠ 실 LLM API는 샌드박스 네트워크 제한으로 호출 불가 → 결정적 부분(그래프 배선·라우팅·차단기)만 `SLICE_OFFLINE` 스텁으로 검증, LLM 노드는 사용자 맥으로 분리(silent mock 금지, 스위치로 명시). (constgx/agents/orchestrator_slice.ipynb)
- 2026-06-18 — [도구특성] 코워크 샌드박스 MCP 파이프라인 검증법: `pip install --break-system-packages langchain-mcp-adapters mcp`(0.3.0/1.28.0). 서버=`mcp.server.fastmcp.FastMCP` + `@mcp.tool()` + `mcp.run(transport="stdio")`. 클라=`MultiServerMCPClient({"name":{"command":"python3","args":["server.py"],"transport":"stdio","env":dict(os.environ)}})` → `await client.get_tools()` → `await tool.ainvoke({...})`. ⚠반환은 **content 블록 리스트** → `json.loads(r[0]["text"])`로 파싱. 서버 서브프로세스 env는 `dict(os.environ)` 넘겨 PATH·플래그 상속(안 넘기면 minimal env). 샌드박스는 외부 검색(ddgs/startpage) **네트워크 차단** → 서버에 `*_OFFLINE` 듀얼모드(고정 스니펫) 넣어 MCP 전송·전체 흐름만 검증, 라이브 검색은 사용자 맥에서. (constgx/agents/mcp_model_tool.py)
- 2026-06-18 — [도구특성] 비동기 LangGraph: async 노드(예 TOOL이 `await mcp_search`)를 쓰면 `app.ainvoke` 필요 → 그땐 **sync `SqliteSaver` 안 됨**(에러: "use AsyncSqliteSaver/install aiosqlite"). `from langgraph.checkpoint.sqlite.aio import AsyncSqliteSaver` + `aiosqlite`로 `conn=await aiosqlite.connect(":memory:"); AsyncSqliteSaver(conn)`. 노트북/셀 **top-level await**는 ipykernel·nbconvert 모두 지원(가정 말고 미니 노트북으로 1회 확인함). sync·async 노드 혼합은 ainvoke 하에서 공존 가능.
- 2026-06-19 — [도구특성] 노트북→.py 모듈 졸업 패턴: ①async 노드가 있으면 build_app도 async(`conn=await aiosqlite.connect(":memory:")` → `AsyncSqliteSaver(conn)`), 진입점 run.py에 `asyncio.run()` 래퍼(노트북 top-level await가 .py에선 불가). ②노트북의 dotenv 자동로드는 모듈에선 run.py 상단에서 `load_dotenv(Path(__file__).resolve().parent.parent.parent/"scripts"/".env")` 명시. ③MCP 서버 경로는 `Path(__file__).resolve().parent.parent/"mcp_model_tool.py"`로 절대화 + `command=sys.executable`(서버 서브프로세스 인터프리터 일치). ④패키지 분리: state/llm/mcp_tools/nodes/orchestrator(route+build_app)/run/util, DIAGNOSIS는 orchestrator. ⑤동등성 검증=`python -m pkg.run`을 OFFLINE 플래그로 돌려 노트북 출력과 대조(A=DONE/B=FAILED). (constgx/agents/orc/)
- 2026-06-20 — [도구특성] AI Hub 단건 다수 적재의 소요시간은 **용량이 아니라 건수×건당 오버헤드**가 지배(전송 시작·압축해제·gsutil 업로드 왕복이 매 filekey 반복). 단일전송 throttle로 같은 크기도 피크시간 3~7배 변동. 71918+71921 = 639건 ~15GB인데 작은 파일 다수라 오버헤드가 총시간 결정. → 일정 추정은 GB가 아니라 filekey 건수 기준으로. (프로젝트 불문 재사용 = AI Hub 적재 일반 특성)
<!-- 예) yt-dlp 자막은 429가 잦음 → ko json3 직접 받아 파싱하면 우회 가능. -->
